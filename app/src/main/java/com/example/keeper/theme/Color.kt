package com.example.keeper.theme

import androidx.compose.ui.graphics.Color

// Google Keep-style palette — a fixed light look, no Material You.
val KeepWhite = Color(0xFFFFFFFF)
val KeepInk = Color(0xFF202124)     // Google's near-black text grey
val KeepYellow = Color(0xFFFEEFC3)  // Keep's signature soft yellow (FAB, selection)
val KeepGreyBg = Color(0xFFF1F3F4)
val KeepGreyText = Color(0xFF5F6368)
val KeepBorder = Color(0xFFE0E0E0)
